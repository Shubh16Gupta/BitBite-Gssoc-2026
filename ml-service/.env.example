# ===========================================
# FarmTrust Crop-Health Service — configuration
# Copy to `.env` (auto-loaded) or export directly.
# ===========================================

# Server
HOST=0.0.0.0
PORT=8000

# Sampling
SAMPLE_COUNT=10
PATCH_FRACTION=0.35
MIN_PATCH_PX=48

# Image fetching guards
IMAGE_FETCH_TIMEOUT=10
MAX_IMAGE_BYTES=15728640
